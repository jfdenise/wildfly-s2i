name: concurrent testing images POC
on:
  push:
    branches:
      - "concurrent_testing"
env:
  LANG: en_US.UTF-8
  S2I_URI: https://github.com/openshift/source-to-image/releases/download/v1.3.1/source-to-image-v1.3.1-a5a77147-linux-amd64.tar.gz
  WILDFLY_S2I_BUILDER_IMAGE: test-wildfly-s2i
  WILDFLY_S2I_RUNTIME_IMAGE: test-wildfly-runtime
  V2_QUAY_REPO: ${{ secrets.V2_TEST_QUAY_REPO }}
  V2_QUAY_USERNAME: ${{ secrets.V2_TEST_QUAY_USERNAME }}
  QUAY_WILDFLY_S2I_BUILDER_IMAGE: quay.io/${{ secrets.V2_TEST_QUAY_REPO }}/test-wildfly-s2i
  QUAY_WILDFLY_S2I_RUNTIME_IMAGE: quay.io/${{ secrets.V2_TEST_QUAY_REPO }}/test-wildfly-runtime
  IMAGE_ID: ${{ github.sha }}
  BEHAVE_TIMEOUT: 60
jobs:
  build_builder_image:
    name: Wildfly-s2i build builder
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest]
        jdk: [jdk21, jdk25]
    steps:
      - name: Update hosts - linux
        if: matrix.os == 'ubuntu-latest'
        run: |
          cat /etc/hosts
          sudo bash -c "echo '127.0.0.1   localhost localhost.localdomain localhost4 localhost4.localdomain4' > /etc/hosts"
          sudo bash -c "echo '::1         localhost localhost.localdomain localhost6 localhost6.localdomain6' >> /etc/hosts"
          sudo sysctl -w fs.file-max=2097152
      - uses: actions/checkout@v2
      - name: Check quay.io configuration
        if: (env.V2_QUAY_USERNAME == '' || env.V2_QUAY_REPO == '')
        run: |
          echo "quay.io configuration is incomplete, can't push to quay.io. To push built images to quay.io, please ensure the secrets V2_TEST_QUAY_REPO, V2_TEST_QUAY_USERNAME and V2_TEST_QUAY_PASSWORD are created in the project."
          exit 1
      - name: Install qemu dependency
        run: |
          sudo apt-get update
          sudo apt-get install -y qemu-user-static
      - name: Setup required system packages
        run: |
          sudo apt-get update
          sudo apt-get install krb5-multidev libkrb5-dev
      - name: Verify latest ubi10-minimal
        run: |
          podman pull registry.access.redhat.com/ubi10/ubi-minimal
          podman image ls | grep ubi10
      - name: Setup Python 3.x
        uses: actions/setup-python@v2
        with:
          python-version: '3.11.5'
      - name: Setup virtualenv and install cekit and required packages
        run: |
          python --version
          sudo pip install virtualenv
          mkdir ~/cekit
          python3 -m venv ~/cekit
          . ~/cekit/bin/activate
          pip install cekit docker docker-squash odcs behave lxml packaging
      - name: install s2i binary
        run: |
          echo ===== Installing s2i from ${{ env.S2I_URL }} =====
          mkdir /tmp/s2i/ && cd /tmp/s2i/
          wget ${{ env.S2I_URI }}
           tar xvf source-to-image*.gz
           sudo mv s2i /usr/bin
           which s2i
           s2i version
      - name: Build DockerFile file
        run: |
          echo "Docker file prior to build wildfly-s2i builder image"
          podman image ls
          . ~/cekit/bin/activate
          pushd wildfly-builder-image
          QUAY_IMAGE_VERSION="latest-${{ matrix.jdk }}-${{ env.IMAGE_ID }}"
          echo "QUAY_IMAGE_VERSION=${QUAY_IMAGE_VERSION}" >> $GITHUB_ENV
          cekit build --dry-run --overrides=${{ matrix.jdk }}-overrides.yaml docker
          popd
      - name: Buildah multi archs Builder Build
        id: build_builder_image
        uses: redhat-actions/buildah-build@v2.10
        with:
          image: ${{ env.WILDFLY_S2I_BUILDER_IMAGE }}
          tags: ${{ env.QUAY_IMAGE_VERSION }}
          context: ./wildfly-builder-image/target/image
          platforms: linux/amd64, linux/arm64, linux/ppc64le
          containerfiles: |
            ./wildfly-builder-image/target/image/Dockerfile
      - name: Push Builder Image To Quay
        uses: redhat-actions/push-to-registry@v2.6
        id: push-builder
        with:
          image: ${{ steps.build_builder_image.outputs.image }}
          tags: ${{ steps.build_builder_image.outputs.tags }}
          registry: quay.io/${{ env.V2_QUAY_REPO }}
          username: ${{ secrets.V2_TEST_QUAY_USERNAME }}
          password: ${{ secrets.V2_TEST_QUAY_PASSWORD }}
  build_runtime_image:
    name: Wildfly-s2i build runtime
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest]
        jdk: [jdk21, jdk25]
    steps:
      - name: Update hosts - linux
        if: matrix.os == 'ubuntu-latest'
        run: |
          cat /etc/hosts
          sudo bash -c "echo '127.0.0.1   localhost localhost.localdomain localhost4 localhost4.localdomain4' > /etc/hosts"
          sudo bash -c "echo '::1         localhost localhost.localdomain localhost6 localhost6.localdomain6' >> /etc/hosts"
          sudo sysctl -w fs.file-max=2097152
      - uses: actions/checkout@v2
      - name: Check quay.io configuration
        if: (env.V2_QUAY_USERNAME == '' || env.V2_QUAY_REPO == '')
        run: |
          echo "quay.io configuration is incomplete, can't push to quay.io. To push built images to quay.io, please ensure the secrets V2_TEST_QUAY_REPO, V2_TEST_QUAY_USERNAME and V2_TEST_QUAY_PASSWORD are created in the project."
          exit 1
      - name: Install qemu dependency
        run: |
          sudo apt-get update
          sudo apt-get install -y qemu-user-static
      - name: Setup required system packages
        run: |
          sudo apt-get update
          sudo apt-get install krb5-multidev libkrb5-dev
      - name: Verify latest ubi10-minimal
        run: |
          podman pull registry.access.redhat.com/ubi10/ubi-minimal
          podman image ls | grep ubi10
      - name: Setup Python 3.x
        uses: actions/setup-python@v2
        with:
          python-version: '3.11.5'
      - name: Setup virtualenv and install cekit and required packages
        run: |
          python --version
          sudo pip install virtualenv
          mkdir ~/cekit
          python3 -m venv ~/cekit
          . ~/cekit/bin/activate
          pip install cekit docker docker-squash odcs behave lxml packaging
      - name: install s2i binary
        run: |
          echo ===== Installing s2i from ${{ env.S2I_URL }} =====
          mkdir /tmp/s2i/ && cd /tmp/s2i/
          wget ${{ env.S2I_URI }}
           tar xvf source-to-image*.gz
           sudo mv s2i /usr/bin
           which s2i
           s2i version
      - name: Build DockerFile files
        run: |
          echo "Docker Images prior to build wildfly-s2i images"
          podman image ls
          . ~/cekit/bin/activate
          pushd wildfly-runtime-image
          QUAY_IMAGE_VERSION="latest-${{ matrix.jdk }}-${{ env.IMAGE_ID }}"
          echo "QUAY_IMAGE_VERSION=${QUAY_IMAGE_VERSION}" >> $GITHUB_ENV
          cekit build --dry-run --overrides=${{ matrix.jdk }}-overrides.yaml docker
          popd
      - name: Buildah multi archs Runtime Build
        id: build_runtime_image
        uses: redhat-actions/buildah-build@v2.10
        with:
          image: ${{ env.WILDFLY_S2I_RUNTIME_IMAGE }}
          tags: ${{ env.QUAY_IMAGE_VERSION }}
          context: ./wildfly-runtime-image/target/image
          platforms: linux/amd64, linux/arm64, linux/ppc64le
          containerfiles: |
            ./wildfly-runtime-image/target/image/Dockerfile
      - name: Push Runtime Image To Quay
        uses: redhat-actions/push-to-registry@v2.6
        id: push-runtime
        with:
          image: ${{ steps.build_runtime_image.outputs.image }}
          tags: ${{ steps.build_runtime_image.outputs.tags }}
          registry: quay.io/${{ env.V2_QUAY_REPO }}
          username: ${{ secrets.V2_TEST_QUAY_USERNAME }}
          password: ${{ secrets.V2_TEST_QUAY_PASSWORD }}
  basic_test:
    name: basic tests
    runs-on: ${{ matrix.os }}
    needs: build_builder_image
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest]
        jdk: [jdk21, jdk25]
    steps:
      - uses: actions/checkout@v2
      - name: Setup Python 3.x
        uses: actions/setup-python@v2
        with:
          python-version: '3.11.5'
      - name: Setup virtualenv and install cekit and required packages
        run: |
          python --version
          sudo pip install virtualenv
          mkdir ~/cekit
          python3 -m venv ~/cekit
          . ~/cekit/bin/activate
          pip install cekit docker docker-squash odcs behave lxml packaging
      - name: install s2i binary
        run: |
          echo ===== Installing s2i from ${{ env.S2I_URL }} =====
          mkdir /tmp/s2i/ && cd /tmp/s2i/
          wget ${{ env.S2I_URI }}
           tar xvf source-to-image*.gz
           sudo mv s2i /usr/bin
           which s2i
           s2i version
      - name: Behave Tests basic
        run: |
          . ~/cekit/bin/activate
          pushd wildfly-builder-image
          docker pull ${{ env.QUAY_WILDFLY_S2I_BUILDER_IMAGE }}:latest-${{ matrix.jdk }}-${{ env.IMAGE_ID }}
          cekit test --image=${{ env.QUAY_WILDFLY_S2I_BUILDER_IMAGE }}:latest-${{ matrix.jdk }}-${{ env.IMAGE_ID }} behave --include-re 1-basic > test-logs-1-basic-${{ matrix.os }}-${{ matrix.jdk }}.txt 2>&1
          rm test-logs-1-basic-${{ matrix.os }}-${{ matrix.jdk }}.txt
          popd
          docker system prune -f
      - name: Advertise Failing Behave Scenarios
        if: failure()
        run: |
          pushd wildfly-builder-image
          logFile=$(find . -type f -iname "test-logs-*.txt")
          if [ -f "${logFile}" ]; then
            # Remove tar noise
            grep -v "tar.go:" ${logFile} > tmpFile && mv tmpFile ${logFile}
            # Display failing scenario
            grep -A10000 "Errored scenarios" ${logFile}
          else
            echo "No failure in behave tests"
          fi
          popd
      - name: Upload Test Reports on Failure
        uses: actions/upload-artifact@v4
        if: failure()
        with:
          name: wildfly-s2i-test-logs-${{ matrix.os }}-${{ matrix.jdk }}
          path: |
           wildfly-builder-image/test-logs-*.txt
           /tmp/*.wfs2i.log
  basic2_test:
    name: basic tests 2
    runs-on: ${{ matrix.os }}
    needs: build_builder_image
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest]
        jdk: [jdk21, jdk25]
    steps:
      - uses: actions/checkout@v2
      - name: Setup Python 3.x
        uses: actions/setup-python@v2
        with:
          python-version: '3.11.5'
      - name: Setup virtualenv and install cekit and required packages
        run: |
          python --version
          sudo pip install virtualenv
          mkdir ~/cekit
          python3 -m venv ~/cekit
          . ~/cekit/bin/activate
          pip install cekit docker docker-squash odcs behave lxml packaging
      - name: install s2i binary
        run: |
          echo ===== Installing s2i from ${{ env.S2I_URL }} =====
          mkdir /tmp/s2i/ && cd /tmp/s2i/
          wget ${{ env.S2I_URI }}
           tar xvf source-to-image*.gz
           sudo mv s2i /usr/bin
           which s2i
           s2i version
      - name: Behave Tests basic 2
        run: |
          . ~/cekit/bin/activate
          pushd wildfly-builder-image
          docker pull ${{ env.QUAY_WILDFLY_S2I_BUILDER_IMAGE }}:latest-${{ matrix.jdk }}-${{ env.IMAGE_ID }}
          cekit test --image=${{ env.QUAY_WILDFLY_S2I_BUILDER_IMAGE }}:latest-${{ matrix.jdk }}-${{ env.IMAGE_ID }} behave --include-re 2-basic > test-logs-2-basic-${{ matrix.os }}-${{ matrix.jdk }}.txt 2>&1
          rm test-logs-2-basic-${{ matrix.os }}-${{ matrix.jdk }}.txt
          popd
          docker system prune -f
      - name: Advertise Failing Behave Scenarios
        if: failure()
        run: |
          pushd wildfly-builder-image
          logFile=$(find . -type f -iname "test-logs-*.txt")
          if [ -f "${logFile}" ]; then
            # Remove tar noise
            grep -v "tar.go:" ${logFile} > tmpFile && mv tmpFile ${logFile}
            # Display failing scenario
            grep -A10000 "Errored scenarios" ${logFile}
          else
            echo "No failure in behave tests"
          fi
          popd
      - name: Upload Test Reports on Failure
        uses: actions/upload-artifact@v4
        if: failure()
        with:
          name: wildfly-s2i-test-logs-${{ matrix.os }}-${{ matrix.jdk }}
          path: |
           wildfly-builder-image/test-logs-*.txt
           /tmp/*.wfs2i.log
  datasource_test:
    name: datasource tests
    runs-on: ${{ matrix.os }}
    needs: build_builder_image
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest]
        jdk: [jdk21, jdk25]
    steps:
      - uses: actions/checkout@v2
      - name: Setup Python 3.x
        uses: actions/setup-python@v2
        with:
          python-version: '3.11.5'
      - name: Setup virtualenv and install cekit and required packages
        run: |
          python --version
          sudo pip install virtualenv
          mkdir ~/cekit
          python3 -m venv ~/cekit
          . ~/cekit/bin/activate
          pip install cekit docker docker-squash odcs behave lxml packaging
      - name: install s2i binary
        run: |
          echo ===== Installing s2i from ${{ env.S2I_URL }} =====
          mkdir /tmp/s2i/ && cd /tmp/s2i/
          wget ${{ env.S2I_URI }}
           tar xvf source-to-image*.gz
           sudo mv s2i /usr/bin
           which s2i
           s2i version
      - name: Behave Tests datasources
        run: |
          . ~/cekit/bin/activate
          pushd wildfly-builder-image
          docker pull ${{ env.QUAY_WILDFLY_S2I_BUILDER_IMAGE }}:latest-${{ matrix.jdk }}-${{ env.IMAGE_ID }}
          cekit test --image=${{ env.QUAY_WILDFLY_S2I_BUILDER_IMAGE }}:latest-${{ matrix.jdk }}-${{ env.IMAGE_ID }} behave --include-re datasources > test-logs-datasources-${{ matrix.os }}-${{ matrix.jdk }}.txt 2>&1
          rm test-logs-datasources-${{ matrix.os }}-${{ matrix.jdk }}.txt
          popd
          docker system prune -f
      - name: Advertise Failing Behave Scenarios
        if: failure()
        run: |
          pushd wildfly-builder-image
          logFile=$(find . -type f -iname "test-logs-*.txt")
          if [ -f "${logFile}" ]; then
            # Remove tar noise
            grep -v "tar.go:" ${logFile} > tmpFile && mv tmpFile ${logFile}
            # Display failing scenario
            grep -A10000 "Errored scenarios" ${logFile}
          else
            echo "No failure in behave tests"
          fi
          popd
      - name: Upload Test Reports on Failure
        uses: actions/upload-artifact@v4
        if: failure()
        with:
          name: wildfly-s2i-test-logs-${{ matrix.os }}-${{ matrix.jdk }}
          path: |
           wildfly-builder-image/test-logs-*.txt
           /tmp/*.wfs2i.log
  extensions_test:
    name: extensions tests
    runs-on: ${{ matrix.os }}
    needs: build_builder_image
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest]
        jdk: [jdk21, jdk25]
    steps:
      - uses: actions/checkout@v2
      - name: Setup Python 3.x
        uses: actions/setup-python@v2
        with:
          python-version: '3.11.5'
      - name: Setup virtualenv and install cekit and required packages
        run: |
          python --version
          sudo pip install virtualenv
          mkdir ~/cekit
          python3 -m venv ~/cekit
          . ~/cekit/bin/activate
          pip install cekit docker docker-squash odcs behave lxml packaging
      - name: install s2i binary
        run: |
          echo ===== Installing s2i from ${{ env.S2I_URL }} =====
          mkdir /tmp/s2i/ && cd /tmp/s2i/
          wget ${{ env.S2I_URI }}
           tar xvf source-to-image*.gz
           sudo mv s2i /usr/bin
           which s2i
           s2i version
      - name: Behave Tests extensions
        run: |
          . ~/cekit/bin/activate
          pushd wildfly-builder-image
          docker pull ${{ env.QUAY_WILDFLY_S2I_BUILDER_IMAGE }}:latest-${{ matrix.jdk }}-${{ env.IMAGE_ID }}
          cekit test --image=${{ env.QUAY_WILDFLY_S2I_BUILDER_IMAGE }}:latest-${{ matrix.jdk }}-${{ env.IMAGE_ID }} behave --include-re extensions > test-logs-extensions-${{ matrix.os }}-${{ matrix.jdk }}.txt 2>&1
          rm test-logs-extensions-${{ matrix.os }}-${{ matrix.jdk }}.txt
          popd
          docker system prune -f
      - name: Advertise Failing Behave Scenarios
        if: failure()
        run: |
          pushd wildfly-builder-image
          logFile=$(find . -type f -iname "test-logs-*.txt")
          if [ -f "${logFile}" ]; then
            # Remove tar noise
            grep -v "tar.go:" ${logFile} > tmpFile && mv tmpFile ${logFile}
            # Display failing scenario
            grep -A10000 "Errored scenarios" ${logFile}
          else
            echo "No failure in behave tests"
          fi
          popd
      - name: Upload Test Reports on Failure
        uses: actions/upload-artifact@v4
        if: failure()
        with:
          name: wildfly-s2i-test-logs-${{ matrix.os }}-${{ matrix.jdk }}
          path: |
           wildfly-builder-image/test-logs-*.txt
           /tmp/*.wfs2i.log
  jgroups_test:
    name: jgroups tests
    runs-on: ${{ matrix.os }}
    needs: build_builder_image
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest]
        jdk: [jdk21, jdk25]
    steps:
      - uses: actions/checkout@v2
      - name: Setup Python 3.x
        uses: actions/setup-python@v2
        with:
          python-version: '3.11.5'
      - name: Setup virtualenv and install cekit and required packages
        run: |
          python --version
          sudo pip install virtualenv
          mkdir ~/cekit
          python3 -m venv ~/cekit
          . ~/cekit/bin/activate
          pip install cekit docker docker-squash odcs behave lxml packaging
      - name: install s2i binary
        run: |
          echo ===== Installing s2i from ${{ env.S2I_URL }} =====
          mkdir /tmp/s2i/ && cd /tmp/s2i/
          wget ${{ env.S2I_URI }}
           tar xvf source-to-image*.gz
           sudo mv s2i /usr/bin
           which s2i
           s2i version
      - name: Behave Tests jgroups
        run: |
          . ~/cekit/bin/activate
          pushd wildfly-builder-image
          docker pull ${{ env.QUAY_WILDFLY_S2I_BUILDER_IMAGE }}:latest-${{ matrix.jdk }}-${{ env.IMAGE_ID }}
          cekit test --image=${{ env.QUAY_WILDFLY_S2I_BUILDER_IMAGE }}:latest-${{ matrix.jdk }}-${{ env.IMAGE_ID }} behave --include-re jgroups > test-logs-jgroups-${{ matrix.os }}-${{ matrix.jdk }}.txt 2>&1
          rm test-logs-jgroups-${{ matrix.os }}-${{ matrix.jdk }}.txt
          popd
          docker system prune -f
      - name: Advertise Failing Behave Scenarios
        if: failure()
        run: |
          pushd wildfly-builder-image
          logFile=$(find . -type f -iname "test-logs-*.txt")
          if [ -f "${logFile}" ]; then
            # Remove tar noise
            grep -v "tar.go:" ${logFile} > tmpFile && mv tmpFile ${logFile}
            # Display failing scenario
            grep -A10000 "Errored scenarios" ${logFile}
          else
            echo "No failure in behave tests"
          fi
          popd
      - name: Upload Test Reports on Failure
        uses: actions/upload-artifact@v4
        if: failure()
        with:
          name: wildfly-s2i-test-logs-${{ matrix.os }}-${{ matrix.jdk }}
          path: |
           wildfly-builder-image/test-logs-*.txt
           /tmp/*.wfs2i.log
  keycloak_test:
    name: keycloak tests
    runs-on: ${{ matrix.os }}
    needs: build_builder_image
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest]
        jdk: [jdk21, jdk25]
    steps:
      - uses: actions/checkout@v2
      - name: Setup Python 3.x
        uses: actions/setup-python@v2
        with:
          python-version: '3.11.5'
      - name: Setup virtualenv and install cekit and required packages
        run: |
          python --version
          sudo pip install virtualenv
          mkdir ~/cekit
          python3 -m venv ~/cekit
          . ~/cekit/bin/activate
          pip install cekit docker docker-squash odcs behave lxml packaging
      - name: install s2i binary
        run: |
          echo ===== Installing s2i from ${{ env.S2I_URL }} =====
          mkdir /tmp/s2i/ && cd /tmp/s2i/
          wget ${{ env.S2I_URI }}
           tar xvf source-to-image*.gz
           sudo mv s2i /usr/bin
           which s2i
           s2i version
      - name: Behave Tests keycloak
        run: |
          . ~/cekit/bin/activate
          pushd wildfly-builder-image
          docker pull ${{ env.QUAY_WILDFLY_S2I_BUILDER_IMAGE }}:latest-${{ matrix.jdk }}-${{ env.IMAGE_ID }}
          cekit test --image=${{ env.QUAY_WILDFLY_S2I_BUILDER_IMAGE }}:latest-${{ matrix.jdk }}-${{ env.IMAGE_ID }} behave --include-re keycloak > test-logs-keycloak-${{ matrix.os }}-${{ matrix.jdk }}.txt 2>&1
          rm test-logs-keycloak-${{ matrix.os }}-${{ matrix.jdk }}.txt
          popd
          docker system prune -f
      - name: Advertise Failing Behave Scenarios
        if: failure()
        run: |
          pushd wildfly-builder-image
          logFile=$(find . -type f -iname "test-logs-*.txt")
          if [ -f "${logFile}" ]; then
            # Remove tar noise
            grep -v "tar.go:" ${logFile} > tmpFile && mv tmpFile ${logFile}
            # Display failing scenario
            grep -A10000 "Errored scenarios" ${logFile}
          else
            echo "No failure in behave tests"
          fi
          popd
      - name: Upload Test Reports on Failure
        uses: actions/upload-artifact@v4
        if: failure()
        with:
          name: wildfly-s2i-test-logs-${{ matrix.os }}-${{ matrix.jdk }}
          path: |
           wildfly-builder-image/test-logs-*.txt
           /tmp/*.wfs2i.log
  legacy_elytron_test:
    name: legacy elytron tests
    runs-on: ${{ matrix.os }}
    needs: build_builder_image
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest]
        jdk: [jdk21, jdk25]
    steps:
      - uses: actions/checkout@v2
      - name: Setup Python 3.x
        uses: actions/setup-python@v2
        with:
          python-version: '3.11.5'
      - name: Setup virtualenv and install cekit and required packages
        run: |
          python --version
          sudo pip install virtualenv
          mkdir ~/cekit
          python3 -m venv ~/cekit
          . ~/cekit/bin/activate
          pip install cekit docker docker-squash odcs behave lxml packaging
      - name: install s2i binary
        run: |
          echo ===== Installing s2i from ${{ env.S2I_URL }} =====
          mkdir /tmp/s2i/ && cd /tmp/s2i/
          wget ${{ env.S2I_URI }}
           tar xvf source-to-image*.gz
           sudo mv s2i /usr/bin
           which s2i
           s2i version
      - name: Behave Tests legacy-elytron
        run: |
          . ~/cekit/bin/activate
          pushd wildfly-builder-image
          docker pull ${{ env.QUAY_WILDFLY_S2I_BUILDER_IMAGE }}:latest-${{ matrix.jdk }}-${{ env.IMAGE_ID }}
          cekit test --image=${{ env.QUAY_WILDFLY_S2I_BUILDER_IMAGE }}:latest-${{ matrix.jdk }}-${{ env.IMAGE_ID }} behave --include-re legacy-elytron > test-logs-legacy-elytron-${{ matrix.os }}-${{ matrix.jdk }}.txt 2>&1
          rm test-logs-legacy-elytron-${{ matrix.os }}-${{ matrix.jdk }}.txt
          popd
          docker system prune -f
      - name: Advertise Failing Behave Scenarios
        if: failure()
        run: |
          pushd wildfly-builder-image
          logFile=$(find . -type f -iname "test-logs-*.txt")
          if [ -f "${logFile}" ]; then
            # Remove tar noise
            grep -v "tar.go:" ${logFile} > tmpFile && mv tmpFile ${logFile}
            # Display failing scenario
            grep -A10000 "Errored scenarios" ${logFile}
          else
            echo "No failure in behave tests"
          fi
          popd
      - name: Upload Test Reports on Failure
        uses: actions/upload-artifact@v4
        if: failure()
        with:
          name: wildfly-s2i-test-logs-${{ matrix.os }}-${{ matrix.jdk }}
          path: |
           wildfly-builder-image/test-logs-*.txt
           /tmp/*.wfs2i.log
  legacy_s2i_test:
    name: legacy s2i tests
    runs-on: ${{ matrix.os }}
    needs: build_builder_image
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest]
        jdk: [jdk21, jdk25]
    steps:
      - uses: actions/checkout@v2
      - name: Setup Python 3.x
        uses: actions/setup-python@v2
        with:
          python-version: '3.11.5'
      - name: Setup virtualenv and install cekit and required packages
        run: |
          python --version
          sudo pip install virtualenv
          mkdir ~/cekit
          python3 -m venv ~/cekit
          . ~/cekit/bin/activate
          pip install cekit docker docker-squash odcs behave lxml packaging
      - name: install s2i binary
        run: |
          echo ===== Installing s2i from ${{ env.S2I_URL }} =====
          mkdir /tmp/s2i/ && cd /tmp/s2i/
          wget ${{ env.S2I_URI }}
           tar xvf source-to-image*.gz
           sudo mv s2i /usr/bin
           which s2i
           s2i version
      - name: Behave Tests legacy-s2i
        run: |
          . ~/cekit/bin/activate
          pushd wildfly-builder-image
          docker pull ${{ env.QUAY_WILDFLY_S2I_BUILDER_IMAGE }}:latest-${{ matrix.jdk }}-${{ env.IMAGE_ID }}
          cekit test --image=${{ env.QUAY_WILDFLY_S2I_BUILDER_IMAGE }}:latest-${{ matrix.jdk }}-${{ env.IMAGE_ID }} behave --include-re legacy-s2i > test-logs-legacy-s2i-${{ matrix.os }}-${{ matrix.jdk }}.txt 2>&1
          rm test-logs-legacy-s2i-${{ matrix.os }}-${{ matrix.jdk }}.txt
          popd
          docker system prune -f
      - name: Advertise Failing Behave Scenarios
        if: failure()
        run: |
          pushd wildfly-builder-image
          logFile=$(find . -type f -iname "test-logs-*.txt")
          if [ -f "${logFile}" ]; then
            # Remove tar noise
            grep -v "tar.go:" ${logFile} > tmpFile && mv tmpFile ${logFile}
            # Display failing scenario
            grep -A10000 "Errored scenarios" ${logFile}
          else
            echo "No failure in behave tests"
          fi
          popd
      - name: Upload Test Reports on Failure
        uses: actions/upload-artifact@v4
        if: failure()
        with:
          name: wildfly-s2i-test-logs-${{ matrix.os }}-${{ matrix.jdk }}
          path: |
           wildfly-builder-image/test-logs-*.txt
           /tmp/*.wfs2i.log
  messaging_test:
    name: messaging tests
    runs-on: ${{ matrix.os }}
    needs: build_builder_image
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest]
        jdk: [jdk21, jdk25]
    steps:
      - uses: actions/checkout@v2
      - name: Setup Python 3.x
        uses: actions/setup-python@v2
        with:
          python-version: '3.11.5'
      - name: Setup virtualenv and install cekit and required packages
        run: |
          python --version
          sudo pip install virtualenv
          mkdir ~/cekit
          python3 -m venv ~/cekit
          . ~/cekit/bin/activate
          pip install cekit docker docker-squash odcs behave lxml packaging
      - name: install s2i binary
        run: |
          echo ===== Installing s2i from ${{ env.S2I_URL }} =====
          mkdir /tmp/s2i/ && cd /tmp/s2i/
          wget ${{ env.S2I_URI }}
           tar xvf source-to-image*.gz
           sudo mv s2i /usr/bin
           which s2i
           s2i version
      - name: Behave Tests messaging
        run: |
          . ~/cekit/bin/activate
          pushd wildfly-builder-image
          docker pull ${{ env.QUAY_WILDFLY_S2I_BUILDER_IMAGE }}:latest-${{ matrix.jdk }}-${{ env.IMAGE_ID }}
          cekit test --image=${{ env.QUAY_WILDFLY_S2I_BUILDER_IMAGE }}:latest-${{ matrix.jdk }}-${{ env.IMAGE_ID }} behave --include-re messaging > test-logs-messaging-${{ matrix.os }}-${{ matrix.jdk }}.txt 2>&1
          rm test-logs-messaging-${{ matrix.os }}-${{ matrix.jdk }}.txt
          popd
          docker system prune -f
      - name: Advertise Failing Behave Scenarios
        if: failure()
        run: |
          pushd wildfly-builder-image
          logFile=$(find . -type f -iname "test-logs-*.txt")
          if [ -f "${logFile}" ]; then
            # Remove tar noise
            grep -v "tar.go:" ${logFile} > tmpFile && mv tmpFile ${logFile}
            # Display failing scenario
            grep -A10000 "Errored scenarios" ${logFile}
          else
            echo "No failure in behave tests"
          fi
          popd
      - name: Upload Test Reports on Failure
        uses: actions/upload-artifact@v4
        if: failure()
        with:
          name: wildfly-s2i-test-logs-${{ matrix.os }}-${{ matrix.jdk }}
          path: |
           wildfly-builder-image/test-logs-*.txt
           /tmp/*.wfs2i.log
  oidc_test:
    name: oidc tests
    runs-on: ${{ matrix.os }}
    needs: build_builder_image
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest]
        jdk: [jdk21, jdk25]
    steps:
      - uses: actions/checkout@v2
      - name: Setup Python 3.x
        uses: actions/setup-python@v2
        with:
          python-version: '3.11.5'
      - name: Setup virtualenv and install cekit and required packages
        run: |
          python --version
          sudo pip install virtualenv
          mkdir ~/cekit
          python3 -m venv ~/cekit
          . ~/cekit/bin/activate
          pip install cekit docker docker-squash odcs behave lxml packaging
      - name: install s2i binary
        run: |
          echo ===== Installing s2i from ${{ env.S2I_URL }} =====
          mkdir /tmp/s2i/ && cd /tmp/s2i/
          wget ${{ env.S2I_URI }}
           tar xvf source-to-image*.gz
           sudo mv s2i /usr/bin
           which s2i
           s2i version
      - name: Behave Tests oidc
        run: |
          . ~/cekit/bin/activate
          pushd wildfly-builder-image
          docker pull ${{ env.QUAY_WILDFLY_S2I_BUILDER_IMAGE }}:latest-${{ matrix.jdk }}-${{ env.IMAGE_ID }}
          cekit test --image=${{ env.QUAY_WILDFLY_S2I_BUILDER_IMAGE }}:latest-${{ matrix.jdk }}-${{ env.IMAGE_ID }} behave --include-re oidc > test-logs-oidc-${{ matrix.os }}-${{ matrix.jdk }}.txt 2>&1
          rm test-logs-oidc-${{ matrix.os }}-${{ matrix.jdk }}.txt
          popd
          docker system prune -f
      - name: Advertise Failing Behave Scenarios
        if: failure()
        run: |
          pushd wildfly-builder-image
          logFile=$(find . -type f -iname "test-logs-*.txt")
          if [ -f "${logFile}" ]; then
            # Remove tar noise
            grep -v "tar.go:" ${logFile} > tmpFile && mv tmpFile ${logFile}
            # Display failing scenario
            grep -A10000 "Errored scenarios" ${logFile}
          else
            echo "No failure in behave tests"
          fi
          popd
      - name: Upload Test Reports on Failure
        uses: actions/upload-artifact@v4
        if: failure()
        with:
          name: wildfly-s2i-test-logs-${{ matrix.os }}-${{ matrix.jdk }}
          path: |
           wildfly-builder-image/test-logs-*.txt
           /tmp/*.wfs2i.log
  s2i_test:
    name: s2i tests
    runs-on: ${{ matrix.os }}
    needs: build_builder_image
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest]
        jdk: [jdk21, jdk25]
    steps:
      - uses: actions/checkout@v2
      - name: Setup Python 3.x
        uses: actions/setup-python@v2
        with:
          python-version: '3.11.5'
      - name: Setup virtualenv and install cekit and required packages
        run: |
          python --version
          sudo pip install virtualenv
          mkdir ~/cekit
          python3 -m venv ~/cekit
          . ~/cekit/bin/activate
          pip install cekit docker docker-squash odcs behave lxml packaging
      - name: install s2i binary
        run: |
          echo ===== Installing s2i from ${{ env.S2I_URL }} =====
          mkdir /tmp/s2i/ && cd /tmp/s2i/
          wget ${{ env.S2I_URI }}
           tar xvf source-to-image*.gz
           sudo mv s2i /usr/bin
           which s2i
           s2i version
      - name: Behave Tests s2i
        run: |
          . ~/cekit/bin/activate
          pushd wildfly-builder-image
          docker pull ${{ env.QUAY_WILDFLY_S2I_BUILDER_IMAGE }}:latest-${{ matrix.jdk }}-${{ env.IMAGE_ID }}
          cekit test --image=${{ env.QUAY_WILDFLY_S2I_BUILDER_IMAGE }}:latest-${{ matrix.jdk }}-${{ env.IMAGE_ID }} behave --include-re s2i-tests > test-logs-s2i-tests-${{ matrix.os }}-${{ matrix.jdk }}.txt 2>&1
          rm test-logs-s2i-tests-${{ matrix.os }}-${{ matrix.jdk }}.txt
          popd
          docker system prune -f
      - name: Advertise Failing Behave Scenarios
        if: failure()
        run: |
          pushd wildfly-builder-image
          logFile=$(find . -type f -iname "test-logs-*.txt")
          if [ -f "${logFile}" ]; then
            # Remove tar noise
            grep -v "tar.go:" ${logFile} > tmpFile && mv tmpFile ${logFile}
            # Display failing scenario
            grep -A10000 "Errored scenarios" ${logFile}
          else
            echo "No failure in behave tests"
          fi
          popd
      - name: Upload Test Reports on Failure
        uses: actions/upload-artifact@v4
        if: failure()
        with:
          name: wildfly-s2i-test-logs-${{ matrix.os }}-${{ matrix.jdk }}
          path: |
           wildfly-builder-image/test-logs-*.txt
           /tmp/*.wfs2i.log
  server_builder_test:
    name: server builder tests
    runs-on: ${{ matrix.os }}
    needs: build_builder_image
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest]
        jdk: [jdk21, jdk25]
    steps:
      - uses: actions/checkout@v2
      - name: Setup Python 3.x
        uses: actions/setup-python@v2
        with:
          python-version: '3.11.5'
      - name: Setup virtualenv and install cekit and required packages
        run: |
          python --version
          sudo pip install virtualenv
          mkdir ~/cekit
          python3 -m venv ~/cekit
          . ~/cekit/bin/activate
          pip install cekit docker docker-squash odcs behave lxml packaging
      - name: install s2i binary
        run: |
          echo ===== Installing s2i from ${{ env.S2I_URL }} =====
          mkdir /tmp/s2i/ && cd /tmp/s2i/
          wget ${{ env.S2I_URI }}
           tar xvf source-to-image*.gz
           sudo mv s2i /usr/bin
           which s2i
           s2i version
      - name: Behave Tests server_builder
        run: |
          . ~/cekit/bin/activate
          pushd wildfly-builder-image
          docker pull ${{ env.QUAY_WILDFLY_S2I_BUILDER_IMAGE }}:latest-${{ matrix.jdk }}-${{ env.IMAGE_ID }}
          cekit test --image=${{ env.QUAY_WILDFLY_S2I_BUILDER_IMAGE }}:latest-${{ matrix.jdk }}-${{ env.IMAGE_ID }} behave --include-re server_builder > test-logs-server_builder-${{ matrix.os }}-${{ matrix.jdk }}.txt 2>&1
          rm test-logs-server_builder-${{ matrix.os }}-${{ matrix.jdk }}.txt
          popd
          docker system prune -f
      - name: Advertise Failing Behave Scenarios
        if: failure()
        run: |
          pushd wildfly-builder-image
          logFile=$(find . -type f -iname "test-logs-*.txt")
          if [ -f "${logFile}" ]; then
            # Remove tar noise
            grep -v "tar.go:" ${logFile} > tmpFile && mv tmpFile ${logFile}
            # Display failing scenario
            grep -A10000 "Errored scenarios" ${logFile}
          else
            echo "No failure in behave tests"
          fi
          popd
      - name: Upload Test Reports on Failure
        uses: actions/upload-artifact@v4
        if: failure()
        with:
          name: wildfly-s2i-test-logs-${{ matrix.os }}-${{ matrix.jdk }}
          path: |
           wildfly-builder-image/test-logs-*.txt
           /tmp/*.wfs2i.log
  stability_test:
    name: stability tests
    runs-on: ${{ matrix.os }}
    needs: build_builder_image
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest]
        jdk: [jdk21, jdk25]
    steps:
      - uses: actions/checkout@v2
      - name: Setup Python 3.x
        uses: actions/setup-python@v2
        with:
          python-version: '3.11.5'
      - name: Setup virtualenv and install cekit and required packages
        run: |
          python --version
          sudo pip install virtualenv
          mkdir ~/cekit
          python3 -m venv ~/cekit
          . ~/cekit/bin/activate
          pip install cekit docker docker-squash odcs behave lxml packaging
      - name: install s2i binary
        run: |
          echo ===== Installing s2i from ${{ env.S2I_URL }} =====
          mkdir /tmp/s2i/ && cd /tmp/s2i/
          wget ${{ env.S2I_URI }}
           tar xvf source-to-image*.gz
           sudo mv s2i /usr/bin
           which s2i
           s2i version
      - name: Behave Tests stability
        run: |
          . ~/cekit/bin/activate
          pushd wildfly-builder-image
          docker pull ${{ env.QUAY_WILDFLY_S2I_BUILDER_IMAGE }}:latest-${{ matrix.jdk }}-${{ env.IMAGE_ID }}
          cekit test --image=${{ env.QUAY_WILDFLY_S2I_BUILDER_IMAGE }}:latest-${{ matrix.jdk }}-${{ env.IMAGE_ID }} behave --include-re stability > test-logs-stability-${{ matrix.os }}-${{ matrix.jdk }}.txt 2>&1
          rm test-logs-stability-${{ matrix.os }}-${{ matrix.jdk }}.txt
          popd
          docker system prune -f
      - name: Advertise Failing Behave Scenarios
        if: failure()
        run: |
          pushd wildfly-builder-image
          logFile=$(find . -type f -iname "test-logs-*.txt")
          if [ -f "${logFile}" ]; then
            # Remove tar noise
            grep -v "tar.go:" ${logFile} > tmpFile && mv tmpFile ${logFile}
            # Display failing scenario
            grep -A10000 "Errored scenarios" ${logFile}
          else
            echo "No failure in behave tests"
          fi
          popd
      - name: Upload Test Reports on Failure
        uses: actions/upload-artifact@v4
        if: failure()
        with:
          name: wildfly-s2i-test-logs-${{ matrix.os }}-${{ matrix.jdk }}
          path: |
           wildfly-builder-image/test-logs-*.txt
           /tmp/*.wfs2i.log
  vanilla_test:
    name: vanilla tests
    runs-on: ${{ matrix.os }}
    needs: build_builder_image
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest]
        jdk: [jdk21, jdk25]
    steps:
      - uses: actions/checkout@v2
      - name: Setup Python 3.x
        uses: actions/setup-python@v2
        with:
          python-version: '3.11.5'
      - name: Setup virtualenv and install cekit and required packages
        run: |
          python --version
          sudo pip install virtualenv
          mkdir ~/cekit
          python3 -m venv ~/cekit
          . ~/cekit/bin/activate
          pip install cekit docker docker-squash odcs behave lxml packaging
      - name: install s2i binary
        run: |
          echo ===== Installing s2i from ${{ env.S2I_URL }} =====
          mkdir /tmp/s2i/ && cd /tmp/s2i/
          wget ${{ env.S2I_URI }}
           tar xvf source-to-image*.gz
           sudo mv s2i /usr/bin
           which s2i
           s2i version
      - name: Behave Tests vanilla-basic
        run: |
          . ~/cekit/bin/activate
          pushd wildfly-builder-image
          docker pull ${{ env.QUAY_WILDFLY_S2I_BUILDER_IMAGE }}:latest-${{ matrix.jdk }}-${{ env.IMAGE_ID }}
          cekit test --image=${{ env.QUAY_WILDFLY_S2I_BUILDER_IMAGE }}:latest-${{ matrix.jdk }}-${{ env.IMAGE_ID }} behave --include-re vanilla-basic > test-logs-vanilla-basic-${{ matrix.os }}-${{ matrix.jdk }}.txt 2>&1
          rm test-logs-vanilla-basic-${{ matrix.os }}-${{ matrix.jdk }}.txt
          popd
          docker system prune -f
      - name: Advertise Failing Behave Scenarios
        if: failure()
        run: |
          pushd wildfly-builder-image
          logFile=$(find . -type f -iname "test-logs-*.txt")
          if [ -f "${logFile}" ]; then
            # Remove tar noise
            grep -v "tar.go:" ${logFile} > tmpFile && mv tmpFile ${logFile}
            # Display failing scenario
            grep -A10000 "Errored scenarios" ${logFile}
          else
            echo "No failure in behave tests"
          fi
          popd
      - name: Upload Test Reports on Failure
        uses: actions/upload-artifact@v4
        if: failure()
        with:
          name: wildfly-s2i-test-logs-${{ matrix.os }}-${{ matrix.jdk }}
          path: |
           wildfly-builder-image/test-logs-*.txt
           /tmp/*.wfs2i.log
  additional_test:
    name: additional tests
    runs-on: ${{ matrix.os }}
    needs: [build_builder_image, build_runtime_image]
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest]
        jdk: [jdk21, jdk25]
    steps:
      - uses: actions/checkout@v2
      - name: Setup Python 3.x
        uses: actions/setup-python@v2
        with:
          python-version: '3.11.5'
      - name: Setup virtualenv and install cekit and required packages
        run: |
          python --version
          sudo pip install virtualenv
          mkdir ~/cekit
          python3 -m venv ~/cekit
          . ~/cekit/bin/activate
          pip install cekit docker docker-squash odcs behave lxml packaging
      - name: install s2i binary
        run: |
          echo ===== Installing s2i from ${{ env.S2I_URL }} =====
          mkdir /tmp/s2i/ && cd /tmp/s2i/
          wget ${{ env.S2I_URI }}
           tar xvf source-to-image*.gz
           sudo mv s2i /usr/bin
           which s2i
           s2i version
      - name: Additional Tests
        run: |
          docker pull ${{ env.QUAY_WILDFLY_S2I_BUILDER_IMAGE }}:latest-${{ matrix.jdk }}-${{ env.IMAGE_ID }}
          docker pull ${{ env.QUAY_WILDFLY_S2I_RUNTIME_IMAGE }}:latest-${{ matrix.jdk }}-${{ env.IMAGE_ID }}
          export IMAGE_VERSION=latest-${{ matrix.jdk }}-${{ env.IMAGE_ID }}
          export IMAGE_NAME=${{ env.QUAY_WILDFLY_S2I_BUILDER_IMAGE }}
          export RUNTIME_IMAGE_NAME=${{ env.QUAY_WILDFLY_S2I_RUNTIME_IMAGE }}
          . ~/cekit/bin/activate
          ./test/run
  remove_images:
    name: remove images
    runs-on: ${{ matrix.os }}
    needs: [build_builder_image, build_runtime_image,additional_test,vanilla_test,stability_test,server_builder_test,s2i_test,oidc_test,messaging_test,legacy_s2i_test,legacy_elytron_test,keycloak_test,jgroups_test,extensions_test,datasource_test,basic_test,basic2_test]
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest]
        jdk: [jdk21, jdk25]
    steps:
      - name: Delete image tag
        run: |
          skopeo  delete --creds "${{ secrets.V2_TEST_QUAY_USERNAME }}:${{ secrets.V2_TEST_QUAY_PASSWORD }}" docker://${{ env.QUAY_WILDFLY_S2I_BUILDER_IMAGE }}:latest-${{ matrix.jdk }}-${{ env.IMAGE_ID }}
          skopeo  delete --creds "${{ secrets.V2_TEST_QUAY_USERNAME }}:${{ secrets.V2_TEST_QUAY_PASSWORD }}" docker://${{ env.QUAY_WILDFLY_S2I_RUNTIME_IMAGE }}:latest-${{ matrix.jdk }}-${{ env.IMAGE_ID }}